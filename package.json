{
  "name": "draw-a-card-for-life",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot ./src/server.ts",
    "build": "bun build ./src/index.html --outdir ./dist --target browser --minify",
    "preview": "bun ./src/server.ts",
    "tauri": "bunx @tauri-apps/cli",
    "app:dev": "bunx @tauri-apps/cli dev",
    "app:build": "bun run build && bunx @tauri-apps/cli build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0",
    "typescript": "^5.9.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "@tauri-apps/cli": "^2.9.0"
  }
}
